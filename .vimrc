set mouse=a "enables the mouse
set showmode "shows the mode in wich we are
set incsearch "does incremental searching: updates the search with the string we are typing in
set hlsearch "higlights search words found
set ignorecase "ingnores the case for a search
set number "enables line number
set history=100 "keeps 100 lines from the command line history
set backup "keeps a backup file
set tabstop=3 "sets a tab to 3 spaces
set t_Co=256 "uses 255 colors 
set ch=2 "sets the command line 2 lines from the fist line of the fileof the file 
set showmatch "shows parenthesis that matches whenever the cursor is on them
